# HTTP Server Configuration
# Port number for the server to listen on (default: 8080)
port=8080

# Document root directory for serving files (default: ./static)
document_root=./static

# Thread pool size for handling concurrent connections
thread_pool_size=10

# Maximum buffer size for requests/responses in bytes
buffer_size=1024

# Log file location
log_file=server_log.txt